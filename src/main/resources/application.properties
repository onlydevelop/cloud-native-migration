spring.datasource.url=jdbc:h2:file:./data/orderdb
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
server.port=8080

# Hardcoded — a config smell we'll fix later
payment.gateway.url=http://localhost:9001/charge
notification.smtp.host=localhost
notification.smtp.port=25

# Local file storage — another smell
invoice.storage.path=/tmp/invoices